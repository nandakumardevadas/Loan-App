const _ = require('lodash');
class SettlementTransformer {
    transformList(data) {
        let transformationArray = [];
        // _.each(data, (value, key) => {
        // });
        return data;
    }
}

module.exports = new SettlementTransformer();