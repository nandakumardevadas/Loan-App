const _ = require('lodash');
class PayeeTransformer {
    transformList(data) {
        let transformationArray = [];
        // _.each(data, (value, key) => {
        // });
        return data;
    }
}

module.exports = new PayeeTransformer();