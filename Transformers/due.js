const _ = require('lodash');
class DueTransformer {
    transformList(data) {
        let transformationArray = [];
        // _.each(data, (value, key) => {
        // });
        return data;
    }
}

module.exports = new DueTransformer();